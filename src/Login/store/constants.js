import { createConstants } from 'main/components/Utils';

export default createConstants(
    'LOGIN_ATTEMPT',
    'LOGIN_SUCCESSFUL',
    'LOGIN_FAILED',
    'USERNAME_CHANGED',
    'PASSWORD_CHANGED'
);
